# примеры.md — Примеры кода

## Публикация в ВК
```python
import os
import requests
from dotenv import load_dotenv

load_dotenv()  # Загружаем .env файл
token = os.getenv("VK_TOKEN")
group_id = os.getenv("VK_GROUP_ID")

def post_to_vk(text):
    url = "https://api.vk.com/method/wall.post"
    params = {
        "owner_id": f"-{group_id}",
        "message": text,
        "access_token": token,
        "v": "5.131"
    }
    response = requests.get(url, params=params)
    return response.json()
```

## Пример .env файла (НИКОГДА не пушить в GitHub!)
```
VK_TOKEN=your_token_here
VK_GROUP_ID=your_group_id_here
```

## Пример .gitignore
```
.env
__pycache__/
*.pyc
```
